baseURL = 'https://recomm-hub.github.io/my-blog/'
languageCode = 'ja'
title = 'おすすめレビュー研究所'
theme = 'hugo-theme-stack'

# 日本語環境の設定
hasCJKLanguage = true
defaultContentLanguage = 'ja'

# ビルド設定
enableRobotsTXT = true
buildDrafts = false
buildFuture = false
buildExpired = false

# ページネーション
[pagination]
  pagerSize = 10

# パーマリンク
[permalinks]
  posts = '/posts/:slug/'
  page = '/:slug/'

# SEO用：サイトマップ自動生成
[sitemap]
  changefreq = 'weekly'
  priority = 0.5

# ── Stack テーマの設定 ──
[params]
  mainSections = ['posts']
  featuredImageField = 'image'
  rssFullContent = true
  description = '暮らしに役立つ商品を徹底比較！おすすめアイテムをランキング形式でご紹介します。'

  [params.footer]
    since = 2026
    customText = '© おすすめレビュー研究所'

  [params.dateFormat]
    published = '2006年1月2日'
    lastUpdated = '2006年1月2日 15:04'

  # サイドバー設定
  [params.sidebar]
    emoji = '📝'
    subtitle = '暮らしに役立つ商品を徹底比較！おすすめアイテムをランキング形式でご紹介します。'
    avatar = ''
    compact = false

  # 記事設定
  [params.article]
    headingAnchor = true
    math = false
    toc = true
    readingTime = true

    [params.article.list]
      showTags = true

    [params.article.license]
      enabled = false

  # サイドバーウィジェット（ホームページ）
  [[params.widgets.homepage]]
    type = 'search'

  [[params.widgets.homepage]]
    type = 'categories'
    [params.widgets.homepage.params]
      limit = 10

  [[params.widgets.homepage]]
    type = 'tag-cloud'
    [params.widgets.homepage.params]
      limit = 20

  [[params.widgets.homepage]]
    type = 'archives'
    [params.widgets.homepage.params]
      limit = 5

  # サイドバーウィジェット（記事ページ）
  [[params.widgets.page]]
    type = 'toc'

  # カラースキーム
  [params.colorScheme]
    toggle = true
    default = 'auto'

  # OGP設定
  [params.opengraph.twitter]
    site = ''
    card = 'summary_large_image'

  # 画像処理
  [params.imageProcessing.cover]
    enabled = true

  [params.imageProcessing.content]
    enabled = true

  # コメント無効
  [params.comments]
    enabled = false

# メニュー（サイドバー）
[[menu.main]]
  identifier = 'home'
  name = 'ホーム'
  url = '/'
  weight = -100
  [menu.main.params]
    icon = 'home'

[[menu.social]]
  identifier = 'rss'
  name = 'RSS'
  url = '/my-blog/index.xml'
  [menu.social.params]
    icon = 'rss'

# 出力フォーマット
[outputs]
  home = ['HTML', 'RSS']

# マークアップ設定
[markup]
  [markup.highlight]
    noClasses = false
    codeFences = true
    guessSyntax = true
    lineNos = false
    lineNumbersInTable = true
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.tableOfContents]
    endLevel = 4
    ordered = true
    startLevel = 2
